# Inherited from gbajs3 upstream. Kept here for tools that scan this folder.
# NOTE: dist/ is intentionally NOT ignored here — the built React app ships in
# the repo so `python app.py` works without a build step on a fresh clone.

logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
coverage/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
